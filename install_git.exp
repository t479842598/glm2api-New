#!/usr/bin/expect -f
set timeout 120
spawn ssh -o StrictHostKeyChecking=no root@182.92.127.90
expect "password:"
send "TANGlidong24ban!\r"
expect "# "
send "dnf install -y git 2>/dev/null || yum install -y git 2>/dev/null\r"
expect -timeout 60 "# "
send "git --version\r"
expect "# "
send "exit\r"
expect eof

#!/usr/bin/expect -f
set timeout 120
spawn ssh -o StrictHostKeyChecking=no root@182.92.127.90
expect "password:"
send "TANGlidong24ban!\r"
expect "# "
send "which docker && docker --version || (curl -fsSL https://get.docker.com | sh && systemctl start docker && systemctl enable docker)\r"
expect -timeout 120 "# "
send "docker --version\r"
expect "# "
send "exit\r"
expect eof

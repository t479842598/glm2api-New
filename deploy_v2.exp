#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@23.95.78.148
expect "password:"
send "TANGlidong24ban\r"
expect -timeout 10 "# "
send "cd /opt/glm2api\r"
expect "# "
send "cp .env /tmp/e.bak\r"
expect "# "
send "git pull origin main 2>&1 | tail -3\r"
expect -timeout 30 "# "
send "cp /tmp/e.bak .env\r"
expect "# "
send "pip3 install -e . --break-system-packages -q 2>&1 | tail -2\r"
expect -timeout 30 "# "
send "systemctl restart glm2api\r"
expect "# "
send "sleep 2 && systemctl is-active glm2api && curl -s https://glm2api.274747.xyz/admin/api/session\r"
expect -timeout 15 "# "
send "exit\r"
expect eof
